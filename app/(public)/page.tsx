// "use client"; // Keeping it commented out for now to see if server rendering basic HTML works

export default function LandingPage() {
  return (
    <div>
      <h1>Simplified Public Landing Page</h1>
      <p>If you see this, the basic page structure is rendering on the server.</p>
      <p>The original content has been temporarily removed to help diagnose a 500 error.</p>
    </div>
  );
}
